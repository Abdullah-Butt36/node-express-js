 // when we use ES6 modules (import , export) we should use file extension .mjs.
 // and we need to write type : module in package.json file
 
 export function Print(){
    setTimeout(() => {
        console.log("ES6 modules Running Successfully");
 })
 }